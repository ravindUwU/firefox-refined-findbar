{
	"name": "firefox-refined-findbar",
	"scripts": {
		"build": "sass ./src/refined-findbar.scss:./dist/refined-findbar.css --no-source-map --no-error-css",
		"watch": "yarn run build -w",
		"clean": "rimraf ./dist",
		"pretty:check": "prettier -c .",
		"pretty:write": "prettier -w .",
		"sass": "sass",
		"prettier": "prettier"
	},
	"devDependencies": {
		"prettier": "^2.8.0",
		"rimraf": "^3.0.2",
		"sass": "^1.56.1"
	}
}
